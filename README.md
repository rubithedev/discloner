# DISCLONER

A simple CLI tool to clone your disks.

## Build

1. Install [`mise`](https://mise.jdx.dev)
2. Run `mise install`. (Trust the configuration file if needed)
3. Run `zig build`

The output binary file will be placed at `./zig-out/bin/discloner`
